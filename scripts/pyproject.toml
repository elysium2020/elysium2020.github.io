[project]
name = "scripts"
version = "0.1.0"
description = "Helper for create new post"
classifiers = ["Private :: Do Not Upload"]
readme = "README.md"
requires-python = ">=3.13.3"
dependencies = ["questionary>=2.1.0", "typer>=0.15.3"]
